# Tasks

- [x] **Task 1: Update Import in index.astro**
  - Change the import statement for `founderPortrait` to point to `../assets/images/gritones-founder.webp`.
  - Verify that the `HeroNoir` component correctly receives the new image asset.

- [x] **Task 2: Validation**
  - Run the dev server to confirm the image displays correctly.
  - Check the `EditorialPortrait` rendering for any unexpected cropping or aspect ratio issues.
  - Verify accessibility (alt text) remains descriptive.
