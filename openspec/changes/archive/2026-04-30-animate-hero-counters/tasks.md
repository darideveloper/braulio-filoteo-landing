# Tasks: Animate Hero Counters

- [x] Identify counters in Hero and HeroNoir sections.
- [x] Update `StatItem.astro` with `js-stat-value` class and `data-value` attribute.
- [x] Update `HeroNoir.astro` stat box with `js-stat-value` class and `data-value` attribute.
- [x] Implement `animateCounters` helper in `Hero.astro`.
- [x] Integrate `animateCounters` into `Hero.astro` entrance timeline (synchronized with scroll indicator).
- [x] Implement `animateCounters` helper in `HeroNoir.astro`.
- [x] Integrate `animateCounters` into `HeroNoir.astro` scroll timeline (synchronized with portrait).
- [x] Fix: Update `animateCounters` to set initial value to "0" before starting animation.
- [x] Validation: Verify counters start at 0 and reach target values.
