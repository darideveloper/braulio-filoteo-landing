---
import clsx from 'clsx';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<p class={clsx(
  "border-l-2 border-brand-primary pl-6 py-2 italic",
  className
)}>
  <slot />
</p>